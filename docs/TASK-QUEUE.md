# قائمة المهام — AUHMC 2026 Task Queue

> هذا الملف يُقرأ في بداية كل جلسة عمل من قبل أنظمة الذكاء الاصطناعي.
>
> **الترتيب:** المهام النشطة في الأعلى، المهام المنجزة في الأسفل.
> **الحالة:** ⏳ قيد الانتظار | 🔄 قيد التنفيذ | ✅ منجز | ❌ ملغى
>
> **آخر تحديث:** 2026-07-17 11:54 (Asia/Damascus)

---

## 🔴 DO THIS NOW (الأولوية القصوى)

### [1] ♻️ إصلاح قاعدة البيانات — sql.js لا يُنشئ ملف على القرص

| التفصيل | القيمة |
|---|---|
| **الحالة** | 🔄 قيد التنفيذ |
| **الأولوية** | 🔴 عاجلة جداً |
| **تاريخ الإضافة** | 2026-07-17 |

#### المهام:
- [ ] **1.1** إصلاح `saveDatabase()` — تحقق من أن `db.export()` يرجع بيانات صحيحة
- [ ] **1.2** إصلاح `seedDatabase()` — تحقق من أن البيانات الافتراضية تُدرج
- [ ] **1.3** إصلاح `db.transaction()` — استبدل باستدعاءات مباشرة لـ `db.run()` إذا لزم الأمر
- [ ] **1.4** تحقق من إنشاء `data/auhmc.db` على القرص بعد التشغيل

**السبب:** الخادم يعمل لكن البيانات فارغة. `sql.js` في الذاكرة فقط ولا يحفظ تلقائياً.

---

## 🟢 المهام النشطة (Pending Tasks)

### [10] 🧹 متابعة التنظيف بعد إعادة الهيكلة

| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **الأولوية** | 🟡 متوسطة |
| **تاريخ الإضافة** | 2026-07-17 |
| **تاريخ الإنجاز** | 2026-07-17 |

#### المهام:
- [x] **10.1** إنشاء مجلد `src/frontend/uploads/` وملف `.gitkeep`
- [x] **10.2** تشغيل `npm start` واختبار الموقع محلياً بعد التنظيف
- [x] **10.3** التحقق من عدم وجود ملفات معلّقة
- [x] **10.4** توثيق جميع التغييرات في CHANGELOG.md

---

### [9] 🔧 إصلاح الأخطاء الحرجة وتنظيم الكود (Code Audit Fixes)

| التفصيل | القيمة |
|---|---|
| **الحالة** | 🔄 قيد التنفيذ |
| **الأولوية** | 🔴 عاجلة |
| **تاريخ الإضافة** | 2026-07-16 |

#### المهام:
- [x] **9.1** إزالة المسار المكرر `/api/submissions` من `server.js`
- [x] **9.2** إضافة دالة `apiPatch` المفقودة في `admin.js`
- [x] **9.3** إضافة دوال `_openIconPickerForTrack` و `_openIconPickerForCommittee` في `admin.js`
- [x] **9.4** إكمال `setupPosters()` و `setupAdminNav()` في `admin.js`
- [x] **9.5** توحيد تخزين الورش (workshops) — إصلاح عدم التوافق بين `join(', ')` و `JSON.parse`
- [x] **9.6** إزالة `require('fs')` المكرر داخل multer في `server.js`
- [x] **9.7** إضافة `media` (logo, bgImage) إلى API response في `content.js`
- [x] **9.8** ربط الـ media بين admin.js و app.js
- [x] **9.9** إنشاء `docs/AI-CONFIG.md` — ملف موحد لمفاتيح وإعدادات الذكاء الاصطناعي
- [x] **9.10** اختبار الموقع محلياً بعد الإصلاحات
- [x] **9.11** إصلاح `overlayOpacity` — استخدام `data.media` بدلاً من `data.hero` في `admin.js`
- [x] **9.12** إضافة `MEDIA` إلى `app.js` مع `applyMedia()` وربطها في `init()`
- [x] **9.13** إضافة حقول `media_*` إلى `saveData()` في `admin.js`

---

### [8] 📋 توصيات تحسين المشروع (من مراجعة الكود - 2026-07-15)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ⏳ قيد الانتظار |
| **الأولوية** | 🟢 منخفضة — تحسينات مستقبلية |
| **تاريخ الإضافة** | 2026-07-15 |

#### المهام:
- [ ] **8.1** توحيد طريقة الاتصال بالـ API في admin.js: استخدام `apiRequest` في كل مكان بدلاً من `fetch` المباشر
- [ ] **8.2** إضافة Cache للبيانات في الـ Frontend (تجنب إعادة تحميل كل شيء في كل مرة)
- [ ] **8.3** إضافة نموذج لتحميل ملفات البحث العلمي (File Upload للأبحاث المقدمة)
- [ ] **8.4** تحسين معالجة الأخطاء وعرضها بشكل أكثر وضوحاً للمستخدم
- [ ] **8.5** إعادة هيكلة `database.js` إلى مجلد `db/` مع ملفات منفصلة
- [ ] **8.6** تحسين أداء `saveDatabase()` بجعلها متكررة (Debounced)
- [ ] **8.7** إضافة صفحة المعرض التفاعلي للبوسترات مع تكبير الصور (Lightbox)
- [ ] **8.8** إضافة قسم "المتحدثون" في الموقع الرئيسي

---

### ~~[0] 🚨 الإصلاحات العاجلة — مرحلة الصفر (Phase 0: Critical Fixes)~~

| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **تاريخ الإنجاز** | 2026-07-17 |

#### المهام:
- [x] **0.1** إصلاح SQL Injection في `database.js` (دالة `isEmpty()` تستخدم interpolated string) ✅
- [x] **0.2** إضافة `await getDatabase()` في كل endpoints في `routes/content.js` (موجود بالفعل في middleware) ✅
- [x] **0.3** إصلاح Admin Track edit: حقل `trackId` لا يُقرأ بشكل صحيح أثناء التعديل ✅
- [x] **0.4** تنظيف الـ routes: إزالة individual endpoints غير المستخدمة ✅
- [x] **0.5** إزالة الكود المكرر من `app.js` (أكثر من 150 سطراً مكرراً) ✅

---

### [1] ♻️ إصلاح قاعدة البيانات — sql.js لا يُنشئ ملف على القرص

| التفصيل | القيمة |
|---|---|
| **الحالة** | 🔄 قيد التنفيذ |
| **الأولوية** | 🔴 عاجلة جداً |
| **تاريخ الإضافة** | 2026-07-17 |

#### المهام:
- [ ] **1.1** إصلاح `saveDatabase()` — تحقق من أن `db.export()` يرجع بيانات صحيحة
- [ ] **1.2** إصلاح `seedDatabase()` — تحقق من أن البيانات الافتراضية تُدرج
- [ ] **1.3** إصلاح `db.transaction()` — استبدل باستدعاءات مباشرة لـ `db.run()` إذا لزم الأمر
- [ ] **1.4** تحقق من إنشاء `data/auhmc.db` على القرص بعد التشغيل

**السبب:** الخادم يعمل لكن البيانات فارغة. `sql.js` في الذاكرة فقط ولا يحفظ تلقائياً.

---

### [2] 🎫 نظام التسجيل الجديد — نوعان (Phase 2: Registration System)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ⏳ قيد الانتظار |
| **الأولوية** | 🔴 عاجلة |
| **تاريخ الإضافة** | 2026-07-15 |

#### المهام:
- [ ] **2.1** تعديل `index.html`: إضافة قسمين للتسجيل (حضور + مشاركة علمية)
- [ ] **2.2** تعديل `app.js`: دالة `setupRegistration()` محدثة تدعم النوعين
- [ ] **2.3** إضافة رفع الملفات عبر multer للتسجيلات (موجود جزئياً في server.js)

---

### [3] 🖼️ تحسينات Frontend (Phase 3: Frontend Improvements)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ⏳ قيد الانتظار |
| **الأولوية** | 🟡 متوسطة |
| **تاريخ الإضافة** | 2026-07-15 |

#### المهام:
- [ ] **3.1** عرض صور الرعاة في الموقع الرئيسي (بدلاً من الأيقونات)
- [ ] **3.2** تحسين معالجة التاريخ (timezone صريح)
- [ ] **3.3** إضافة قسم "المتحدثون" في الموقع
- [ ] **3.4** إضافة خريطة Google Maps لمكان المؤتمر

---

### [4] 🔧 تحسينات لوحة التحكم (Phase 4: Admin Improvements)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ⏳ قيد الانتظار |
| **الأولوية** | 🟡 متوسطة |
| **تاريخ الإضافة** | 2026-07-15 |

#### المهام:
- [ ] **4.1** Admin: إدارة الرعاة مع رفع الشعار والصورة
- [ ] **4.2** Admin: Export CSV/Excel للتسجيلات
- [ ] **4.3** Admin: تحديث واجهة المستخدم (تصميم أفضل لجدول التسجيلات)

---

### [5] 📦 النشر والبناء (Phase 5: Build & Deploy)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ❌ ملغى |
| **الأولوية** | 🟢 منخفضة |
| **تاريخ الإضافة** | 2026-07-14 |
| **تاريخ الإلغاء** | 2026-07-17 |

#### المهام:
- [x] **5.1** ~~تحديث `scripts/build.js` ليشمل الملفات الجديدة~~ — ملغى
- [x] **5.2** ~~اختبار البناء والتأكد من عدم وجود أخطاء~~ — ملغى
- [x] **5.3** ~~تحديث التوثيق (AGENTS.md, ARCHITECTURE.md, DEPLOY-GUIDE.md)~~ — ملغى
- [x] **5.4** ~~رفع الموقع على Plesk~~ — ملغى

**السبب:** تم حذف فكرة deploy بالكامل. الخادم يخدم مباشرة من `src/frontend/` بدون build step.

---

## ✅ المهام المنجزة (Completed Tasks)

### ~~7️⃣ تحسينات الواجهة — إلغاء الوضع الليلي + تحكم بالصورة الخلفية + منتقي الأيقونات~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **تاريخ الإنجاز** | 2026-07-15 |
| **الوصف** | إلغاء Dark Mode، دعم صورة خلفية مع overlay، منتقي أيقونات في Admin |

### ~~3️⃣ معرض البوسترات العلمية (Posters Gallery)~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **تاريخ الإنجاز** | 2026-07-15 |
| **الوصف** | جدول posters في DB، CRUD API في content.js، عرض في app.js، تصميم في styles.css، إدارة في admin.js |

### ~~4️⃣ لوحة التحكم المطوّرة (جزئي)~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز جزئياً |
| **تاريخ الإنجاز** | 2026-07-15 |
| **الوصف** | إدارة البوسترات ✅، عرض التسجيلات ✅، تعديل/حذف التسجيلات ✅، رفع الصور عبر /api/upload ✅ |

### ~~2️⃣ نظام التسجيل الجديد (جزئي)~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز جزئياً |
| **تاريخ الإنجاز** | 2026-07-15 |
| **الوصف** | جدول submissions في DB ✅، API في registrations.js ✅، جدول submission_files ✅ |

### ~~1️⃣ إعادة هيكلة المشروع بالكامل~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **تاريخ الإنجاز** | 2026-07-14 |
| **الوصف** | تنظيف الفوضى: حذف github-pages، scripts النشر القديمة، التوثيق المكرر. إنشاء هيكل src/ + deploy/ + docs/. تنظيف app.js و admin.js من DEFAULTS. إنشاء build script. |

### [11] 📝 توثيق قواعد القراءة والتحديث الإلزامية (2026-07-17)

| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **الأولوية** | 🔴 عاجلة |
| **تاريخ الإضافة** | 2026-07-17 |
| **تاريخ الإنجاز** | 2026-07-17 |

#### المهام:
- [x] **11.1** إضافة قاعدة قراءة جميع ملفات AI إلزامياً في بداية كل جلسة (حتى PLAN MODE)
- [x] **11.2** إضافة قاعدة تحديث تلقائي بعد كل تعديل (CHANGELOG + TASK-QUEUE + ملفات توثيق)
- [x] **11.3** إضافة قاعدة: لا تخرج من PLAN MODE بدون تذكير بطلب التبديل لـ ACT MODE
- [x] **11.4** توثيق هذه القواعد في جميع ملفات AI (AI-CONFIG.md, AGENTS.md, CHANGELOG.md, TASK-QUEUE.md)

### ~~🧹 تنظيف شامل للمشروع + توثيق النشر (2026-07-17)~~
| التفصيل | القيمة |
|---|---|
| **الحالة** | ✅ منجز |
| **تاريخ الإنجاز** | 2026-07-17 |
| **الوصف** | حذف فكرة deploy بالكامل، توحيد مفات AI، إنشاء دليل نشر Plesk، حذف 30+ ملف قديم، إصلاح .gitignore، إصلاح مسارات uploads، توثيق كل المحادثة، حذف Git بالكامل |

---

> 📌 **ملاحظة:** في بداية كل جلسة عمل:
> 1. اقرأ `AI-CONFIG.md` ← ثم `TASK-QUEUE.md` ← ثم `CHANGELOG.md`
> 2. اعرض للمستخدم المهام في طابور الانتظار واسأله إن كان يريد العمل على أي منها
> 3. لا تبدأ بأي مهمة دون موافقة المستخدم